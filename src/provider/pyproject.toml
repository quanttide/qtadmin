[project]
name = "qtadmin-provider"
version = "0.1.0"
description = "qtadmin provider API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.46.0",
]
