name: qtadmin_finance
description: Finance workspace for qtadmin Studio, layered on top of the shared finance adapter.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_quanttide_finance:
    path: ../../../../packages/finance/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

