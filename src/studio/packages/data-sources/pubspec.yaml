name: data_sources
description: Generic data loading infrastructure (DataSource, DataLoader, DataResult). Supports file and asset bundle backends.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
